# ResuLab 项目框架文档

## 架构概览

```
用户浏览器 (SPA)
├── 首页 (/)
│   ├── HeroSection        — 主标题 + CTA
│   ├── TemplateShowcase    — 模板缩略图展示
│   └── FeatureList         — 功能亮点
│
└── 编辑器 (/builder)
    ├── 左侧: FormPanel     — 表单录入区 (480px)
    │   ├── FormSection[]   — 可折叠区域
    │   └── SectionForm[]   — 各模块表单
    │
    └── 右侧: PreviewPanel  — 实时预览区
        ├── PreviewToolbar  — 模板切换/缩放/导出
        ├── PreviewFrame    — A4 纸容器
        └── Template[]      — 模板渲染组件
```

## 数据流

```
用户输入
  → dispatch(action)
    → resumeReducer(state, action) → newState
      → Context 通知所有消费者
        → 表单组件: 更新显示值
        → 预览组件: 重新渲染模板
      → useEffect: 防抖写入 localStorage
```

## 核心类型

参见 `src/types/resume.ts`

- `ResumeData` — 顶层聚合类型
- `PersonalInfo`, `JobIntention` — 基础信息
- `Education`, `WorkExperience`, `Project` — 经历类（数组）
- `Skill`, `Certificate`, `Language` — 技能类（数组）
- `SocialLink` — 链接类（数组）

## 状态管理 Actions

```
SET_PERSONAL_INFO      — 更新基本信息
SET_JOB_INTENTION      — 更新求职意向
ADD_ITEM               — 向数组字段添加条目
UPDATE_ITEM            — 更新数组字段中某条目
REMOVE_ITEM            — 删除数组字段中某条目
MOVE_ITEM              — 调整数组字段顺序
SET_SELF_EVALUATION    — 更新自我评价
SET_TEMPLATE           — 切换模板
LOAD_RESUME            — 从 localStorage 加载
RESET_ALL              — 重置为默认值
```

## 模板开发指南

### 模板接口

```typescript
interface TemplateProps {
  data: ResumeData;
}
```

### 添加新模板步骤

1. 创建 `src/templates/<name>/<Name>Template.tsx`
2. 在 `src/constants/templates.ts` 注册（id, name, component, thumbnail）
3. 在 `src/types/resume.ts` 的 `TemplateId` 类型中添加新 id
4. 添加缩略图到 `public/templates/previews/`

### 模板渲染约束

- 必须在 A4 尺寸内渲染：794px × 1123px (96 DPI)
- 使用绝对单位（px/mm/pt），禁止 vw/vh
- 通过 TemplateBase 组件包裹获得统一容器和打印样式
- 优雅处理空字段：空数组不渲染该区域

## 部署说明

当前为纯客户端 SPA，部署到任意静态托管即可：

```bash
npm run build
# 将 dist/ 目录部署到 Nginx / Vercel / GitHub Pages
```

后续接入后端时，数据模型 `ResumeData` 可直接存储为 MySQL JSON 字段或 MongoDB 文档，reducer 逻辑无需修改。
