# ResuLab 项目框架文档

## 架构概览

```
用户浏览器 (SPA)
├── 首页 (/)
│   ├── HeroSection        — 主标题 + CTA
│   ├── TemplateShowcase    — 模板缩略图展示
│   └── FeatureList         — 功能亮点
│
└── 编辑器 (/builder/:templateId)
    ├── 左侧: FormPanel     — 表单录入区 (440px)
    │   ├── FormSection[]   — 可折叠区域
    │   └── SectionForm[]   — 10 个模块表单
    │
    └── 右侧: PreviewPanel  — 实时预览区
        ├── PreviewToolbar  — 模板切换 / 主题色 / 排版设置 / 智能功能 / 缩放 / 导出
        ├── PreviewFrame    — A4 纸容器 (794×1123px)
        └── Template[]      — 3 个模板组件（按需懒加载）
```

## 数据流

```
用户输入
  → dispatch(action)
    → resumeReducer(state, action) → newState
      → Context 通知所有消费者
        → 表单组件: 更新显示值
        → 预览组件: 重新渲染模板（应用字体/字号/行距/页边距）
      → useEffect: 防抖 300ms 写入 localStorage
```

## 核心类型

参见 `src/types/resume.ts`

- `ResumeData` — 顶层聚合类型，含 templateId, accentColor, fontFamily, fontSize, lineHeight, pageMargin
- `PersonalInfo`, `JobIntention` — 基础信息
- `Education`, `WorkExperience`, `Project` — 经历类（数组，支持排序）
- `Skill`, `Certificate`, `Language` — 技能类（数组）
- `SocialLink` — 链接类（数组）
- `FontFamily` — 'default' | 'serif' | 'mono'
- `PageMargin` — 'narrow' | 'normal' | 'wide'

## 状态管理 Actions

```
SET_PERSONAL_INFO      — 更新基本信息
SET_JOB_INTENTION      — 更新求职意向
ADD_ITEM               — 向数组字段添加条目
UPDATE_ITEM            — 更新数组字段中某条目
REMOVE_ITEM            — 删除数组字段中某条目
MOVE_ITEM             — 调整数组字段顺序
SET_SELF_EVALUATION    — 更新自我评价
SET_TEMPLATE           — 切换模板
SET_ACCENT_COLOR       — 切换主题色
SET_FONT_FAMILY        — 切换字体（default/serif/mono）
SET_FONT_SIZE          — 调整字号（12-18px）
SET_LINE_HEIGHT        — 调整行距（1.4-2.0）
SET_PAGE_MARGIN        — 调整页边距（narrow/normal/wide）
SMART_SORT             — 智能排序（教育/工作/项目按时间排序）
LOAD_RESUME            — 从 localStorage 加载
RESET_ALL              — 重置为默认值
```

## 模板开发指南

### 模板接口

```typescript
interface TemplateProps {
  data: ResumeData;
}
```

模板可通过 `data` 获取所有自定义设置：
- `data.accentColor` — 主题色
- `data.fontFamily` — 字体（映射到 Tailwind font class）
- `data.fontSize` — 字号（px）
- `data.lineHeight` — 行距（倍数）
- `data.pageMargin` — 页边距（narrow/normal/wide）

### 添加新模板步骤

1. 创建 `src/templates/<name>/<Name>Template.tsx`
2. 在 `src/constants/templates.ts` 注册（id, name, description, component, color, hasPhoto）
3. 在 `src/types/resume.ts` 的 `TemplateId` 类型中添加新 id

### 模板渲染约束

- 必须在 A4 尺寸内渲染：794px × 1123px (96 DPI)
- 使用绝对单位（px/mm/pt），禁止 vw/vh
- 通过 TemplateBase 组件包裹获得统一容器和打印样式
- 使用 `data.fontSize` 和 `data.lineHeight` 代替硬编码值
- 页边距通过 `MARGIN_PX` 映射表转为 px 值

## 部署说明

当前为纯客户端 SPA，部署到任意静态托管即可：

```bash
npm run build
# 将 dist/ 目录部署到 Nginx / Vercel / GitHub Pages
```

后续接入后端时，数据模型 `ResumeData` 可直接存储为 MySQL JSON 字段或 MongoDB 文档，reducer 逻辑无需修改。
